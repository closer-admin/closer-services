#
# Routes
#

# Home page
GET     /regions                             com.closer.services.controllers.Application.getAllRegions
POST    /region                                  com.closer.services.controllers.Application.addRegion

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file        controllers.Assets.versioned(path="/public", file)
