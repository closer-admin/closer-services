#
# Routes
#

# Regions
GET           /api/regions                                 @controllers.RegionController.all
GET           /api/regions/:id                             @controllers.RegionController.getById(id)
DELETE        /api/regions/:id                             @controllers.RegionController.removeById(id)
PUT           /api/regions/:id                             @controllers.RegionController.update(id)
POST          /api/regions                                 @controllers.RegionController.save
POST          /api/regions/wraps                           @controllers.RegionController.getInZoneRegions

# Promotions
GET           /api/regions/:regionId/promotions            @controllers.PromotionController.all(regionId)
GET           /api/regions/:regionId/promotions/:id        @controllers.PromotionController.getById(regionId, id)
DELETE        /api/regions/:regionId/promotions/:id        @controllers.PromotionController.removeById(regionId, id)
POST          /api/regions/:regionId/promotions            @controllers.PromotionController.save(regionId)

# Map static resources from the /public folder to the /assets URL path
GET           /assets/*file                                controllers.Assets.versioned(path="/public", file)
